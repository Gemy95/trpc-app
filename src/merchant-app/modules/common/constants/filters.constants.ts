export enum MatchCategories {
  categories = 'categories',
  categoriesIds = 'categoriesIds',
}

export enum MatchProductCategories {
  productcategories = 'productcategories',
  productcategoriesIds = 'productcategoriesIds',
}

export enum MatchCities {
  cities = 'cities',
  citiesIds = 'citiesIds',
}

export enum MatchCountries {
  countries = 'countries',
  countriesIds = 'countriesIds',
}

export enum MatchBranches {
  branches = 'branches',
  branchesIds = 'branchesIds',
}

export enum MatchTags {
  tags = 'tags',
  tagsIds = 'tagsIds',
}

export enum MatchMerchants {
  merchants = 'merchants',
  merchantsIds = 'merchantsIds',
}

export enum MatchOrders {
  orders = 'orders',
  ordersIds = 'ordersIds',
}

export enum MatchTransactions {
  transactions = 'transactions',
  transactionsIds = 'transactionsIds',
}

export enum MatchTables {
  tables = 'tables',
  tablesIds = 'tablesIds',
}

export enum MatchProducts {
  products = 'products',
  productsIds = 'productsIds',
}

export enum MatchRest {
  build_status = 'build_status',
  release_status = 'release_status',
  visibility_status = 'visibility_status',
  status = 'status',
  status_tags = 'status_tags',
  orderType = 'orderType',
  orderRefId = 'orderRefId',
  clientName = 'clientName',
  clientEmail = 'clientEmail',
  clientMobile = 'clientMobile',
  transactionRefId = 'transactionRefId',
  mobile = 'mobile',
  email = 'email',
  client_status = 'client_status',
  stores_status = 'stores_status',
  order = 'order',
  client_visibility = 'client_visibility',
  stores_visibility = 'stores_visibility',
  action = 'action',
  merchantName = 'merchantName',
  ownerEmail = 'ownerEmail',
  ownerMobile = 'ownerMobile',
  operationId = 'operationId',
  clientId = 'clientId',
  branchId = 'branchId',
  merchantId = 'merchantId',
  productStatus = 'productStatus',
  productCategoryStatus = 'productCategoryStatus',
  merchantRequestType = 'merchantRequestType',
  startDate = 'startDate',
  endDate = 'endDate',
  menu_upload_status = 'menu_upload_status',
  isVipTable = 'isVipTable',
  code = 'code',
  free_delivery = 'free_delivery',
  is_new_client = 'is_new_client',
  is_reusable = 'is_reusable',
}

export enum MatchMongoId {
  merchantId = 'merchantId',
  branchId = 'branchId',
}

export enum MatchMongoRegex {
  merchantName = 'merchantName',
}
