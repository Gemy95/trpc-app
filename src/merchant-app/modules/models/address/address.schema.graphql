enum CLIENT_ADDRESS_TYPE {
  home
  work
  friend_home
  rest
  others
}

type Address {
  _id: String
  name: String
  type: CLIENT_ADDRESS_TYPE
  street: String
  client: ObjectId
  location: Location
  locationDelta: [Float]
  isDeleted: Boolean
  floor: Int
  note: String
  city: String
  active: Boolean
  createdAt: String
  updatedAt: String
}